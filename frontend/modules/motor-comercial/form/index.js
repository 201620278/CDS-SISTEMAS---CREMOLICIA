/**
 * Form — Index (versão oficial)
 *
 * Sprint 2.7: Arquitetura Frontend — infraestrutura de formulário.
 *
 * @module frontend/modules/motor-comercial/form
 */

const FormContext = require('./FormContext');
const Validation = require('./Validation');
const DirtyState = require('./DirtyState');

module.exports = {
  FormContext,
  Validation,
  DirtyState
};
