/**
 * Navigation Components — Index (versão oficial)
 *
 * Sprint 2.7: Arquitetura Frontend — componentes de navegação.
 *
 * @module frontend/modules/motor-comercial/components/navigation
 */

const Tabs = require('./Tabs');
const Modal = require('./Modal');
const Stepper = require('./Stepper');

module.exports = {
  Tabs,
  Modal,
  Stepper
};
