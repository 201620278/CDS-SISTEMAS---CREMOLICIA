/**
 * Base Components — Index (versão oficial)
 *
 * Sprint 2.7: Arquitetura Frontend — componentes base.
 *
 * @module frontend/modules/motor-comercial/components/base
 */

const Button = require('./Button');
const Card = require('./Card');
const Badge = require('./Badge');
const Alert = require('./Alert');
const Loading = require('./Loading');
const EmptyState = require('./EmptyState');
const ErrorState = require('./ErrorState');
const Avatar = require('./Avatar');
const Skeleton = require('./Skeleton');
const Divider = require('./Divider');
const Tag = require('./Tag');

module.exports = {
  Button,
  Card,
  Badge,
  Alert,
  Loading,
  EmptyState,
  ErrorState,
  Avatar,
  Skeleton,
  Divider,
  Tag
};
