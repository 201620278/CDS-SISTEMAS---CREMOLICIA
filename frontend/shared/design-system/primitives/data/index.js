/**
 * Data Components — Index (versão oficial)
 *
 * Sprint 2.7: Arquitetura Frontend — componentes de dados.
 *
 * @module frontend/modules/motor-comercial/components/data
 */

const Table = require('./Table');
const Pagination = require('./Pagination');
const StatCard = require('./StatCard');
const Progress = require('./Progress');

module.exports = {
  Table,
  Pagination,
  StatCard,
  Progress
};
