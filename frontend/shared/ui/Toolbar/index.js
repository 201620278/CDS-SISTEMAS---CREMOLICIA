const { createPlannedComponent } = require('../_status');
module.exports = createPlannedComponent('Toolbar', {
  responsibility: 'Ferramentas da área (export, refresh) — não substitui WorkspaceFooter'
});
