const { createPlannedComponent } = require('../_status');
module.exports = createPlannedComponent('CreditStrip', {
  responsibility: 'Faixa única de crédito: disponível / valor operação / restante'
});
