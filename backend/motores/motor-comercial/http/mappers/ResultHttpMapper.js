/**
 * ResultHttpMapper — Converte resultados de UseCase em respostas HTTP.
 *
 * Delega à implementação corporativa compartilhada (sucesso/dados/isOk).
 *
 * @module motores/motor-comercial/http/mappers/ResultHttpMapper
 */

module.exports = require('../../../../shared/http/mappers/ResultHttpMapper');
