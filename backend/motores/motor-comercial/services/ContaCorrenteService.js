/**
 * ContaCorrenteService — Serviço de conta corrente consignada.
 *
 * Sprint 1.1: estrutura reservada — sem lógica de negócio.
 *
 * @class ContaCorrenteService
 */

class ContaCorrenteService {
  /**
   * @param {Object} [deps]
   */
  constructor(deps = {}) {
    this._deps = deps;
  }
}

module.exports = ContaCorrenteService;
