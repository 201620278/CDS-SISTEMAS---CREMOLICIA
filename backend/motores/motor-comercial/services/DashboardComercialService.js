/**
 * DashboardComercialService — Serviço do dashboard comercial.
 *
 * Sprint 1.1: estrutura reservada — sem lógica de negócio.
 *
 * @class DashboardComercialService
 */

class DashboardComercialService {
  /**
   * @param {Object} [deps]
   */
  constructor(deps = {}) {
    this._deps = deps;
  }
}

module.exports = DashboardComercialService;
