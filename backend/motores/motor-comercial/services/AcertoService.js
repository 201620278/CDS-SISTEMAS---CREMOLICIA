/**
 * AcertoService — Serviço de prestação de contas / acertos.
 *
 * Sprint 1.1: estrutura reservada — sem lógica de negócio.
 *
 * @class AcertoService
 */

class AcertoService {
  /**
   * @param {Object} [deps]
   */
  constructor(deps = {}) {
    this._deps = deps;
  }
}

module.exports = AcertoService;
