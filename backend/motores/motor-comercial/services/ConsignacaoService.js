/**
 * ConsignacaoService — Serviço do domínio Consignação.
 *
 * Sprint 1.1: estrutura reservada — sem lógica de negócio.
 *
 * @class ConsignacaoService
 */

class ConsignacaoService {
  /**
   * @param {Object} [deps]
   */
  constructor(deps = {}) {
    this._deps = deps;
  }
}

module.exports = ConsignacaoService;
