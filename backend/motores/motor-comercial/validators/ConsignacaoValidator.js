/**
 * ConsignacaoValidator — Validações de entrada do domínio Consignação.
 *
 * Sprint 1.1: estrutura reservada — sem validações implementadas.
 *
 * @class ConsignacaoValidator
 */

class ConsignacaoValidator {
  /**
   * @param {Object} [deps]
   */
  constructor(deps = {}) {
    this._deps = deps;
  }
}

module.exports = ConsignacaoValidator;
