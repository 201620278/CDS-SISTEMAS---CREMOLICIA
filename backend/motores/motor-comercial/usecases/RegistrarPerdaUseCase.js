/**
 * RegistrarPerdaUseCase — Caso de uso para registro de perda.
 *
 * Sprint 1.1: estrutura reservada — sem lógica de negócio.
 *
 * @class RegistrarPerdaUseCase
 */

class RegistrarPerdaUseCase {
  /**
   * @param {Object} [deps]
   */
  constructor(deps = {}) {
    this._deps = deps;
  }
}

module.exports = RegistrarPerdaUseCase;
