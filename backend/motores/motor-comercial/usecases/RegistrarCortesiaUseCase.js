/**
 * RegistrarCortesiaUseCase — Caso de uso para registro de cortesia.
 *
 * Sprint 1.1: estrutura reservada — sem lógica de negócio.
 *
 * @class RegistrarCortesiaUseCase
 */

class RegistrarCortesiaUseCase {
  /**
   * @param {Object} [deps]
   */
  constructor(deps = {}) {
    this._deps = deps;
  }
}

module.exports = RegistrarCortesiaUseCase;
