/**
 * RealizarAcertoUseCase — Caso de uso para prestação de contas.
 *
 * Sprint 1.1: estrutura reservada — sem lógica de negócio.
 *
 * @class RealizarAcertoUseCase
 */

class RealizarAcertoUseCase {
  /**
   * @param {Object} [deps]
   */
  constructor(deps = {}) {
    this._deps = deps;
  }
}

module.exports = RealizarAcertoUseCase;
