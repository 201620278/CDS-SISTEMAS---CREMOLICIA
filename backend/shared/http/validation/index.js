/**
 * Validation HTTP — Index (versão corporativa).
 *
 * Sprint 2.5.5: Hardening da API — infraestrutura de validação HTTP.
 *
 * @module backend/shared/http/validation
 */

const ValidationPipeline = require('./ValidationPipeline');

module.exports = {
  ValidationPipeline
};
