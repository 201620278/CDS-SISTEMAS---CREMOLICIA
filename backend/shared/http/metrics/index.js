/**
 * Metrics HTTP — Index (versão corporativa).
 *
 * Sprint 2.5.5: Hardening da API — infraestrutura de métricas.
 *
 * @module backend/shared/http/metrics
 */

const MetricsService = require('./MetricsService');

module.exports = {
  MetricsService
};
