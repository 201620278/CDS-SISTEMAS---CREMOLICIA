/**
 * Responses HTTP — Index.
 *
 * Sprint 2.5.5: Hardening da API — infraestrutura compartilhada.
 *
 * @module backend/shared/http/responses
 */

const StandardResponse = require('./StandardResponse');

module.exports = {
  StandardResponse
};
